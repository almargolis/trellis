# Trellis Configuration

# Flask secret key (generate a secure random string for production)
SECRET_KEY=your-secret-key-here

# Site branding (displayed in header, footer, page titles)
SITE_NAME=My Digital Garden
SITE_AUTHOR=Your Name

# Data directory - where database and writable files are stored
DATA_DIR=/path/to/data

# Content directory - where markdown content lives
# Defaults to DATA_DIR/content if not specified
CONTENT_DIR=/path/to/data/content

# Git repository path for auto-commits (optional)
GITLAB_REPO_PATH=.

# Debug mode (set to 'true' for development)
FLASK_DEBUG=false
